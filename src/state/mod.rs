// In src/state/mod.rs

//! Manages the application's persistent state, primarily through database interactions.
//! 
//! This module defines the data structures for job definitions, job runs, and tasks,
//! and provides the `Db` struct for interacting with the underlying database.

pub mod db;
